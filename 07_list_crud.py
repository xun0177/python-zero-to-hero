"""
第7题：列表的增删改查（CRUD 操作）
知识点：列表的创建、append() 添加元素、insert() 插入元素、
        remove() 删除元素、索引访问、切片操作
"""

def list_crud_demo():  # CRUD 是 Create（增）Read（查）Update（改）Delete（删）的缩写
    # ---------- 创建列表 ----------
    fruits = ["苹果", "香蕉", "橙子"]  # 列表用方括号定义，元素用逗号分隔

    # ---------- 增（Create） ----------
    fruits.append('西瓜')  # append() 在列表末尾添加一个元素
    print(fruits)  # 输出：['苹果', '香蕉', '橙子', '西瓜']

    fruits.insert(1, '榴莲')  # insert(索引, 元素) 在指定位置插入元素
    print(fruits)  # 输出：['苹果', '榴莲', '香蕉', '橙子', '西瓜']

    # ---------- 删（Delete） ----------
    fruits.remove('苹果')  # remove(元素) 删除列表中第一个匹配的元素
    print(fruits)  # 输出：['榴莲', '香蕉', '橙子', '西瓜']

    # ---------- 查（Read） ----------
    # 索引访问：用 [索引] 获取指定位置的元素，索引从 0 开始
    print(f'列表索引：{fruits[1]}')  # 获取第 2 个元素，输出：香蕉

    # 切片操作：[起始索引:结束索引] 获取子列表，左闭右开（包含起始，不包含结束）
    print(f'列表切片：{fruits[0:2]}')  # 获取索引 0 到 1 的元素，输出：['榴莲', '香蕉']

# 调用函数执行演示
list_crud_demo()


"""
补充说明

1. 列表的索引规则：
   - 正向索引从 0 开始，fruits[0] 是第一个元素。
   - 反向索引从 -1 开始，fruits[-1] 是最后一个元素。
   - 索引超出范围会报错（IndexError）。

2. append() 与 insert() 的区别：
   - append(元素) 只能在末尾添加，一次添加一个元素。
   - insert(索引, 元素) 可以在任意位置插入，插入后原位置及后续元素自动后移。

3. remove() 的注意事项：
   - 删除列表中第一个匹配的元素。
   - 如果要删除的元素不存在，程序会报错（ValueError）。
   - 本例中 '苹果' 存在于列表中，所以删除成功。

4. 切片操作 [start:end]：
   - 返回从 start 到 end-1 的新列表，原列表不变。
   - 省略 start 表示从开头开始，省略 end 表示直到末尾。
   - 例：fruits[:2] 取前两个，fruits[1:] 取从第 2 个到最后。

5. 修改元素（Update）：
   本例未涉及，但修改语法很简单：fruits[0] = '火龙果' 即可替换第一个元素。

6. 扩展思考：
   如果列表中同时存在两个 '苹果'，remove() 会删除哪一个？
   （提示：只删除第一个匹配项）
"""